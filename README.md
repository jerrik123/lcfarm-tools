# lcfarm-tools
采用spring shell 做的基于cmd的工具集。

利用maven打成zip包。
  lcfarm-tools-bin.zip
    --bin
      --startup.sh
      --shutdown.sh
      --startup.bat
      --shutdown.bat
      
    --config
      --*.properties
      --*.xml
      
    --lib
      --*.jar
     

